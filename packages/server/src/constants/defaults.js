/**
 * 默认请求示例常量
 * 包含PC端和移动端App的默认请求示例
 */

// 默认请求示例
export const DEFAULT_REQUESTS = {
  // PC端默认请求
  pc: '生成一个标准的资源管理界面。筛选条件包含：名称（输入框）和状态（下拉框）。表格显示 ID、名称、状态、创建时间，并包含带有编辑/删除按钮的操作列。需要分页功能。',

  // App移动端默认请求
  app: '生成一个标准的移动端列表页面，顶部有搜索框，中间是商品卡片列表（每个卡片包含商品图片、名称、价格和购买按钮），底部有导航栏（首页、分类、购物车、我的）。支持下拉刷新和上拉加载更多功能。'
}
